# NexHire Frontend Environment Template
# For single-process unified hosting (Render), leave empty or use relative '/api'
# For decoupled static hosting (e.g. Vercel), set to your deployed backend URL:
# VITE_API_URL=https://nexhire-platform.onrender.com/api
VITE_API_URL=/api
