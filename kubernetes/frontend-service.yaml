apiVersion: v1
kind: Service
metadata:
  name: jobboard-frontend
  namespace: jobboard
  labels:
    app: jobboard-frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: jobboard-frontend
